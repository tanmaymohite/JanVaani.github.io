import siteConfig from '../config/siteConfig.json';

export const districtsData = siteConfig.districts;
export const corporationMapping = siteConfig.corporations;
export const categoriesData = siteConfig.categories;
